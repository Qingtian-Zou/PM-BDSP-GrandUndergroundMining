# Grand Underground Mining Game - Backend API
# Python dependencies for Railway deployment

# Use CPU-only PyTorch to reduce image size (~200MB vs ~2GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

flask>=2.3.0
flask-cors>=4.0.0
numpy>=1.24.0
gymnasium>=0.29.0
gunicorn>=21.0.0
