/**
 * Configuration for Grand Underground Mining Game
 * 
 * This file configures the API base URL for connecting to the backend.
 * For GitHub Pages deployment, update API_BASE_URL to your Railway backend URL.
 */

// Backend API URL - Update this to your Railway deployment URL
// Example: 'https://your-app-name.up.railway.app'
const API_BASE_URL = '';

// Export for use in other modules
window.CONFIG = {
    API_BASE_URL: API_BASE_URL,
};
